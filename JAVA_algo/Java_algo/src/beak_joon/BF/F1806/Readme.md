# 부분합
https://www.acmicpc.net/problem/1806

## Solution
**투 포인터 알고리즘**을 사용해야 합니다~  처음 써봐용~  
*투 포인터 알고리즘 이란?*  
*정렬된 리스트를 두 개의 포인터를 이용해 순차적으로 접근하면서, 두 포인터 구간의 값이 타겟 값과 같을 때 까지 포인터를 조작하는 기법.*  
1. 리스트를 오름차순으로 정렬한다  
2. 포인터 `left`는 리스트의 시작점을, 포인터 `right`는 리스트의 끝점을 가리킨다  
3. `left`와 `right`가 만날 때 까지 (= 모든 경우를 확인할 때 까지) 다음을 반복  
3-1) `arr[left]`와 `arr[right]`가 조건에 만족하면, `left++`, `right--`  
3-2) 조건에 안맞는다면 `left++` 혹은 `right--`를 시행  
https://www.acmicpc.net/workbook/view/3264 이거 풀어보자  
  
  
그럼 이 문제는 어떤 배열을, 어떤 조건으로, 언제 left++/right-- 해야 할까?  
