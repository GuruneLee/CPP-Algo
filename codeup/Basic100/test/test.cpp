#include <iostream>
using namespace std;

int a[100][100];
int dx[] = {1,0};
int dy[] = {0,1};

int main() {
    int h,w,n;
    cin >> h >> w >> n;
    while (n--) {
        int l,d;
        cin >> l >> d;
        int x,y;
        cin >> x >> y;
        for (int i=0; i<l; i++) {

        }
    }

    for (int i=0; i<w; i++)
    return 0;
}